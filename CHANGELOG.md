# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-08-14

### 新增

- **命中的词在原文里红色高亮**：正文、昵称、@用户名都会标；命中范围盖到 emoji
  （X 用 `<img alt>` 渲染 emoji）时给 emoji 描一圈红框。
- **鼠标移到红色的词上弹出小卡片**，写着命中的规则原文（正则完整显示），
  点一下「把「xxx」加入白名单」就停用这条规则，页面上所有因它被拦的回复当场恢复。
- 设置里新增开关「红色高亮命中的词」，默认开启。

### 改进

- 高亮定位改为「先把子树摊平成整段文本、匹配后再把下标映射回 DOM 节点」。
  逐个文本节点找是定位不到的：X 会把一条正文切成好几个 `span`，emoji 是 `<img alt>`，
  而命中的可能是 `/^…$/` 这类只在整段文本上成立的正则。
- 面板里「匹配设置」改名为「设置」。

### 修复

- 单条回复处理出错不再中断整页过滤。此前 `flush()` 里没有容错，一个异常会让
  后面所有回复直接漏过，而且表面上看不出来。

## [1.2.0] - 2026-08-13

### 新增

- **第二个词库来源：社区词库**，同步上游开源项目
  [amahteru/x-comment-blocker](https://github.com/amahteru/x-comment-blocker)。
  和内置词库可以分别开关、分别同步；它不随扩展打包，安装后自动获取一次。
- **白名单**：列在里面的规则一律不生效，对三份来源都有效。
  两份词库改为只读快照（同步时整份替换），面板里逐条列出并带搜索框，
  点那一行的「白名单」就能停用某条规则。
- 「恢复默认设置」会保留「我的关键词」和白名单。

### 改进

- 内置 `keywords.txt` 与社区词库去重：删掉 74 行重复内容，只留上游没有的 183 条。
  两条上游被塞了零宽字符的词（`求主␣人`、`主⁠人␣快来`）保留了干净版本 ——
  关掉「忽略隐藏字符」时上游那两条形同废词。
- 词库列表懒渲染，只在展开「逐条查看」时才建 DOM，七百多条不拖慢弹窗打开速度。

## [1.0.0] - 2026-08-12

### 新增

- 初版：在推文详情页按关键词过滤垃圾回复，命中后「变透明」（透明度 0–100% 可调）
  或「隐藏」，只处理回复、不动主楼。
- 内置词库 + 「我的关键词」，普通词与 `/正则/flags` 混排；内置词库可从 GitHub 同步。
- 匹配前归一化，识别「同 城 约」这类插空格、夹零宽字符的拆字规避。
- 只做视觉过滤：不拉黑、不 mute、不调用 X 的任何接口、不读 cookie、不刷新页面。
